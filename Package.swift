// swift-tools-version:5.10
import PackageDescription

let package = Package(
    name: "SwiftConsoleApp",
    targets: [
        .executableTarget(
            name: "SwiftConsoleApp",
            path: "Sources/SwiftConsoleApp"
        )
    ]
)
